{
  "extends": "@folio/eslint-config-stripes",
  "plugins": ["regex"],
  "rules": {
    "import/no-named-as-default": "off"
    // // ensure we use uber-package imports
    // "regex/invalid": [
    //   "error",
    //   [
    //     "import (type\\s)?(.+,?\\s?)?(\\{(.|\\n)+?\\})? from .@folio.stripes[^/]"
    //   ]
    // ]
  },
  "overrides": [
    {
      "files": ["src/test/**/*", "src/**/*.test.ts", "src/**/*.test.tsx"],
      "env": { "jest": true }
    }
  ]
}
